# Dev docker build file

FROM node:22-slim

# Set app directory
WORKDIR /usr/src/app

EXPOSE 4000

ENV PATH /node_modules/.bin:$PATH

ENV NODE_ENV development

RUN corepack enable

# Install app dependencies
COPY package.json ./

RUN pnpm install

CMD [ "pnpm", "dev" ]
