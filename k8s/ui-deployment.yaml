apiVersion: apps/v1
kind: Deployment
metadata:
  name: ui
  namespace: ttis
  labels:
    app: ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ui
  template:
    metadata:
      labels:
        app: ui
    spec:
      containers:
        - name: ui
          image: ttis-ui:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          env:
            - name: HOSTNAME
              value: "0.0.0.0"
            - name: PORT
              value: "3000"
---
apiVersion: v1
kind: Service
metadata:
  name: ui
  namespace: ttis
  labels:
    app: ui
spec:
  selector:
    app: ui
  ports:
    - port: 3000
      targetPort: 3000
